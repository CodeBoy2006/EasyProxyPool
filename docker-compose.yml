version: '3.8'

services:
  easyproxypool:
    build: .
    container_name: easyproxypool
    ports:
      - "17283:17283"  # SOCKS5
      - "17285:17285"  # HTTP/HTTPS (CONNECT)
    volumes:
      - ./config.yaml:/app/config.yaml:ro  # Mount config as read-only
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai  # Set timezone (optional)
      # - LOG_LEVEL=debug  # Override config logging level (optional)
    networks:
      - proxy-network

networks:
  proxy-network:
    driver: bridge
